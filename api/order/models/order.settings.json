{
  "kind": "collectionType",
  "collectionName": "orders",
  "info": {
    "name": "order"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "scheduled": {
      "type": "boolean"
    },
    "appointments": {
      "collection": "appointment",
      "via": "order"
    },
    "payment": {
      "model": "payment",
      "via": "order"
    },
    "patient": {
      "via": "orders",
      "model": "patient"
    },
    "student": {
      "via": "orders",
      "model": "student"
    }
  }
}
